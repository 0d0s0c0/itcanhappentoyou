# Copy to server/.env and fill in. server/.env is gitignored.
#
#     cp server/.env.example server/.env

# Which model categorises incident reports: "anthropic" or "gemini"
AI_PROVIDER=anthropic

# Only the provider selected above is required.
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

PORT=3000
